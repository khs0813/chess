# ChessStep SEO·배포 점검표

이 문서는 Render 배포, 커스텀 도메인 연결, Google Search Console, 네이버 서치어드바이저 등록을 순서대로 점검하기 위한 운영 체크리스트입니다.

## 1. 최초 배포 전

- [ ] Node.js 20 이상에서 `npm ci`가 성공한다.
- [ ] `npm run check`가 오류 없이 끝난다.
- [ ] `.env` 또는 Render 환경변수의 `SITE_URL`이 실제 대표 URL과 정확히 같다.
- [ ] `SITE_URL`은 `https://`를 사용한다.
- [ ] `www` 사용 여부를 하나로 결정했다.
- [ ] 사이트명·설명·콘텐츠에 임시 문구가 남아 있지 않다.
- [ ] `public/og-default.png`를 실제 브랜드 이미지로 교체할지 검토했다.
- [ ] 모바일에서 홈, 대국, 세 코스를 확인했다.
- [ ] 영어 페이지가 단순 자동번역처럼 보이지 않고 자연스럽다.

## 2. Render 설정

- [ ] Build Command는 `npm ci && npm run build`이다.
- [ ] Publish Directory는 `dist`이다.
- [ ] Runtime은 Static이다.
- [ ] `render.yaml`의 서비스 이름이 저장소·브랜드와 맞는다.
- [ ] 배포 로그에서 `Built 22 localized pages`가 출력된다.
- [ ] `/robots.txt`가 200으로 열린다.
- [ ] `/sitemap.xml`이 200으로 열린다.
- [ ] 존재하지 않는 URL이 `404.html`을 보여 주고 HTTP 404를 반환한다.
- [ ] `/ko/`가 `/`로, `/ko/play/`가 `/play/`로 리디렉션된다.

## 3. 커스텀 도메인 연결 후

- [ ] Render에서 도메인 인증과 TLS 발급이 완료되었다.
- [ ] `http://` 요청이 `https://`로 이동한다.
- [ ] 대표 호스트가 아닌 주소가 대표 호스트로 이동하도록 설정했다.
- [ ] `SITE_URL`을 최종 도메인으로 바꾸고 재배포했다.
- [ ] 페이지 소스의 canonical이 최종 도메인을 사용한다.
- [ ] hreflang 링크가 최종 도메인을 사용한다.
- [ ] `robots.txt`의 Sitemap 주소가 최종 도메인을 사용한다.
- [ ] `sitemap.xml`의 모든 `<loc>`가 최종 도메인을 사용한다.
- [ ] 과거 `onrender.com` 주소를 Search Console·네이버에 대표 사이트로 제출하지 않는다.

## 4. 페이지별 기본 확인

아래 한국어·영어 URL을 표본 검사합니다.

- [ ] `/`
- [ ] `/play/`
- [ ] `/learn/beginner/`
- [ ] `/learn/intermediate/`
- [ ] `/learn/advanced/`
- [ ] `/rules/`
- [ ] `/en/`
- [ ] `/en/play/`
- [ ] `/en/learn/beginner/`
- [ ] `/en/rules/`

각 페이지에서 확인할 항목:

- [ ] 고유한 title
- [ ] 페이지 내용을 요약하는 meta description
- [ ] 한 개의 h1
- [ ] 자기 자신을 가리키는 canonical
- [ ] 동일 주제 한국어·영어를 연결하는 hreflang
- [ ] `x-default`가 한국어 기본 URL을 가리킴
- [ ] JSON-LD 문법 오류 없음
- [ ] 내부 링크와 언어 전환 링크 정상
- [ ] 모바일 가로 스크롤 없음
- [ ] 체스판의 버튼과 레슨 체크박스 사용 가능

## 5. Google Search Console

- [ ] 최종 도메인 속성을 DNS로 인증했다.
- [ ] 필요하면 URL 접두어 속성도 함께 만들었다.
- [ ] HTML 메타 태그 인증을 쓰는 경우 `GOOGLE_SITE_VERIFICATION`에 content 값만 넣었다.
- [ ] `https://대표도메인/sitemap.xml`을 제출했다.
- [ ] 사이트맵을 성공적으로 읽고 22개 URL을 발견한다.
- [ ] URL 검사에서 `/`, `/play/`, `/learn/beginner/`, `/en/`을 확인했다.
- [ ] Google이 선택한 canonical과 사용자가 선언한 canonical이 일치한다.
- [ ] 페이지 색인 생성 보고서에서 `robots.txt에 의해 차단됨`이 없다.
- [ ] 중복·대체 페이지가 의도한 hreflang/canonical 구조와 맞는지 본다.
- [ ] 구조화 데이터 검사에서 치명적 오류가 없다.
- [ ] Core Web Vitals와 모바일 사용 편의성을 배포 후 추적한다.

## 6. 네이버 서치어드바이저

- [ ] 최종 대표 도메인을 사이트로 등록했다.
- [ ] `NAVER_SITE_VERIFICATION`에 인증 태그의 content 값만 넣었다.
- [ ] 재배포 후 소유확인이 성공한다.
- [ ] robots.txt 검증에서 루트 수집이 허용된다.
- [ ] 사이트맵에 `https://대표도메인/sitemap.xml`을 제출했다.
- [ ] 한국어 홈과 핵심 페이지를 웹 페이지 수집 요청했다.
- [ ] 사이트 진단의 title·description·robots·사이트맵 오류를 확인했다.
- [ ] 수집 현황과 색인 현황을 매주 기록한다.
- [ ] 같은 URL을 반복 제출하는 대신 콘텐츠 개선과 내부 링크를 우선한다.

## 7. 출시 후 4주 운영

### 매주

- [ ] Google·네이버의 노출수, 클릭수, CTR, 평균 순위를 기록한다.
- [ ] 검색어를 초급 규칙·컴퓨터 체스·전술·오프닝·엔드게임 의도로 분류한다.
- [ ] 노출은 있는데 CTR이 낮은 페이지의 title과 description을 한 번에 하나씩 수정한다.
- [ ] 순위 8~20위 페이지에 예시 포지션, FAQ, 내부 링크를 보강한다.
- [ ] 색인되지 않은 URL이 기술 문제인지 콘텐츠 품질 문제인지 구분한다.
- [ ] 404와 잘못된 리디렉션을 확인한다.

### 콘텐츠 확장 우선순위

1. 초보자가 검색하는 구체적 질문: 캐슬링 조건, 앙파상, 스테일메이트, 기물 가치
2. 전술 패턴별 독립 문서: 포크, 핀, 스큐어, 발견 공격, 백랭크 메이트
3. 오프닝별 입문 문서: 이탈리안, 런던 시스템, 퀸즈 갬빗, 카로칸
4. 엔드게임별 독립 문서: 킹+폰, 룩 메이트, 오포지션, 룩 엔드게임 원칙
5. 플레이 기록·전술 퀴즈 등 재방문 기능

새 페이지를 추가할 때는 단순 키워드 변형 페이지를 만들지 말고, 검색 의도마다 실제로 다른 설명·예제·연습 과제를 제공합니다.

## 8. 수정 후 회귀 검사

```bash
npm run check
```

성공 기준:

- 체스 엔진 테스트 전부 통과
- 22개 이상의 정적 HTML 정상 생성
- SEO 검사 실패 0건
- 내부 링크 오류 0건
- 사이트맵 URL 수와 실제 페이지 수 일치

페이지를 추가했다면 `src/content/site.mjs`의 경로, `scripts/build.mjs`의 빌더, 콘텐츠, 언어 대응 URL을 모두 함께 추가합니다.
